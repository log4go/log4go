# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go Debug binary, build by atom package "go-debug"
debug

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
